package net_test

import "testing"

func TestClient(t *testing.T) {
	/*
		host := "localhost"

		builder := net.NewClientBuilder()

		builder.WithPG(pg.NewClientBuilder())

		client := net.NewClient(host)
	*/

}
