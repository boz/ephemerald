package pool_test

import (
	"testing"
)

func TestResource(t *testing.T) {
	/*
		logrus.SetOutput(os.Stderr)
		logrus.SetLevel(logrus.InfoLevel)
		resource := pool.NewPGResource()
		pool, err := pool.NewPool(resource, 5)
		require.NoError(t, err)
		defer pool.Stop()

		assert.NoError(t, pool.Fetch())

		require.NoError(t, pool.CreateItem())
	*/

}
